server {
    listen 8080;

    access_log /dev/stdout;
    error_log /dev/stdout info;

    location /m/static/user/user-setting {
        alias /home/nginx/static/dist/;
    }

     location /healthz {
            return 200;
    }
}